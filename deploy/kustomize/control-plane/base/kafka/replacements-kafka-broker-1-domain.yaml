source:
  kind: ConfigMap
  name: cno-config
  fieldPath: data.KAFKA_BROKER_1_DOMAIN
targets:
- select:
    kind: kafka
    name: cno-kafka-cluster
  fieldPaths:
  - spec.kafka.listeners.[name=external].configuration.brokers.[broker=1].host