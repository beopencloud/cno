source:
  kind: ConfigMap
  name: cno-config
  fieldPath: data.KAFKA_BOOTSTRAP_DOMAIN
targets:
- select:
    kind: kafka
    name: cno-kafka-cluster
  fieldPaths:
  - spec.kafka.listeners.[name=external].configuration.bootstrap.host