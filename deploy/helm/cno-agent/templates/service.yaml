apiVersion: v1
kind: Service
metadata:
  name: {{ include "cno-agent.fullname" . }}
  labels:
    {{- include "cno-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cno-agent.selectorLabels" . | nindent 4 }}
