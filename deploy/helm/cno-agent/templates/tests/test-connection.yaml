apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cno-agent.fullname" . }}-test-connection"
  labels:
    {{- include "cno-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cno-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
