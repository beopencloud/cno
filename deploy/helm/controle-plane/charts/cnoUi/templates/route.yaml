{{ if (eq .Values.global.expose.type "route") }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: cno-ui
  name: cno-ui
spec:
  host: {{ printf "%s.%s" "cno"  .Values.global.expose.domain }}
  to:
    kind: Service
    name: cno-ui
    weight: 100
  wildcardPolicy: None
  tls:
    termination: edge
{{ end }}