{{- if and (eq .Values.expose.type "nginx-ingress") (eq .Values.cluster.platform "kubernetes") }}
Edit CNO API URL in UI
kubectl -n {{ .Release.Namespace }} svc cno-api
kubectl -n {{ .Release.Namespace }} set env deployment/cno-ui API_URL=<svc cno-api>
{{- else }}
kubectl -n {{ .Release.Namespace }} get ingress cno-api
{{- end }}
